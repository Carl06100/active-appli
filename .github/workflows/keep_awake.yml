name: Keep Streamlit Awake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit App
        run: |
          curl --max-redirs 5 -L "https://medcodex-v2-3n7bazwmm37wt35cx5pvu.streamlit.app/" > /dev/null
