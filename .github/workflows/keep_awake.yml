name: Keep Streamlit Awake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -L --silent --show-error --fail "https://https:/medcodex-v2-3n7bazwwmn37wt35cx5pvu.streamlit.app/" > /dev/null
